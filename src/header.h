//common header file
#ifdef  __INCLUDED__
	
#else
	#include <stdio.h>
	#include <string.h>
	#include <errno.h>
	#include <map>
	#include <string>
	#define __INCLUDED__
	#define SHOW_GUI
	#define __DEBUG__
	#define CAPTURE_FROM_WEBCAM
#endif
